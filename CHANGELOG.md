# 更新日志

## [0.4.34] - 2026-09-18

### 修复

- **子代理提醒时机**：工具调用中间回合不再触发完成或异常提醒，也不会误清理异常状态；只有主对话最终完成时提醒一次。

### 优化

- **调试日志收口**：过滤高频事件，debug.log 超过 8 MB 时只保留最近记录，避免长期运行无限膨胀。

## [0.4.32] - 2026-09-16

### 修复

- **测试通知工具安装兼容性**：优先使用宿主提供的插件目录，兼容非默认安装位置。

## [0.4.31] - 2026-09-16

### 修复

- **子 agent 完成提醒时机**：子 agent 返回期间不再触发聊天完成提醒；等当前对话框的后台任务全部收齐、主对话框完成收尾后再提醒。
- **后台任务终态收口**：识别 `completed`、`failed`、`canceled`、`aborted` 终态，避免宿主延迟 `remove` 事件导致提醒门长期卡住。

## [0.4.29] - 2026-09-16

## [0.4.29] - 2026-09-16

### 修复

- **指定模型调用失败**：Hana 模型档改为读取已配置凭据后由插件直连，避开 `utility:call-text` 无法单次选择 provider/model 的限制。
- **指定模型网络白名单**：将 Hana 当前模型目录中的供应商域名加入 `network.allowedHosts`，修复指定 MiniMax 等模型直连时被宿主拦截的问题。
- **模型调用回归覆盖**：补充 203 条回归测试，覆盖直连、响应解析、思考型模型空正文重试、凭据脱敏和配置迁移。

## [0.4.23] - 2026-08-30

### 新功能

- **动森岛民通知风格（10 套）**：西施惠播报 / 杰克·严肃 / 小润·傲娇 / 茶茶丸·哇耶 / 莫妮卡·呀哈 / 美玲·哦呀 / 艳后·尼罗 / 章丸丸·没错 / 狸克商店 / 豆狸粒狸接龙。口癖在标题，正文尽量留原话，像岛民本人说话。
- **通知风格下拉二级分组**：风格按「日常风格 / 动森岛民 / 整活玩法」分组折叠展示，组内可点组头收合；默认只展开当前选中项所在组，选起来不眼花。
- **设置页助手可隐藏**：点「管理助手」弹层用勾选控制列表显示（勾=显示，不勾=隐藏，数据一直保留），不删任何文件、不影响其他插件。
- **断联提醒独立开关**：设置页新增「断联提醒」（回复异常中断时弹通知）开关，默认开；配了解语花悬浮球的用户可关掉避免双份提醒。

### 优化

- **动森风格交叉审查优化**（审查伙伴三维度：资料准确性 / 语感贴合 / 玩家视角）：章丸丸口癖「认同→没错」（简中官方口癖）、小润标题统一「哼」并修复润色 prompt 与规则版人设分裂、狸克标题加铃钱梗（他是商人不是店员）、艳后「尼罗→尼罗哟」软化、美玲「哎呀→哦呀」更贴大小姐、豆狸粒狸去掉句尾「光临」保留双人接力、杰克正文零词缀惜字如金、茶茶丸/莫妮卡/西施惠口癖去重只留标题。
- **动森风格专属预览样例**：10 套风格配「看看效果」专属文案，不再 fallback 默认文案。
- **滚动条主题化**：页面全局细薄荷圆条（8px 横竖同规范），beautify-select 积木滚动条同步新口径。

### 修复

- **润色模型输出安全拒绝文案误弹**：识别「抱歉，这个话题我不能帮你」这类拒绝文案为润色失败并降级规则版，不再把拒绝文案当正常通知弹给用户。
- **管理隐藏列表不显示**：设置页「管理隐藏助手」此前永远提示空列表（前端未接 hiddenAgents 字段），已修复并收敛为「管理助手」勾选弹层。

## [0.4.16] - 2026-08-22

### 新功能

- **活跃回合追踪 + 重启补弹**：Hana 进程被杀/崩溃重启时，补一条「上次对话被打断」提醒；正常退出不误报，只提醒最近 6 小时内的中断。
- **异常回合提醒**：监听自动重试耗尽、provider 错误、会话不健康和非主动超时释放，按会话去重；复用现有弹窗、音效、会话标题和文案润色，不自动续接、不注入消息。
- **个别助手单独设置**：每个助手可单独覆盖通知时机（跟随全局/四档），并入「按助手个性化」卡片。
- **计划任务接管总开关**：可整体开启/关闭计划任务提醒，并按宿主事件时序安全去重；同时修复聊天回复合并状态被刷新配置清掉的问题。
- **系统通知停留时长**：设置页可选「系统默认 / Windows 原生长时长」两档。

### 修复

- **润色稳定性**：润色失败自动重试一次（追加「必须输出 JSON」硬指令），JSON 解析容错加强（剥 Markdown 代码块、杂文本抠 JSON 对象），润色提示词加输出示例与「思考完必须输出 JSON」硬约束，maxTokens 300→500，缓解模型只思考不输出正文导致的降级。
- **通知文案漏思考链**：stripMetaBlocks 新增 `<think>...` 与 `<thinking>...</thinking>` 块剥离，覆盖截断残留兜底。
- **新窗口第一条回复缺标题**：会话标题查询挪到润色之后并带一次短重试，修复宿主异步起名来不及落地导致的时序问题。
- **反馈弹窗重做**：全新 tg-feedback 外壳，纯色直角面板、无虚线无灰影，恢复 18px 圆角。
- **按助手个性化卡片**：加宽卡片并收紧网格列，修复「看看效果」按钮在卡片右侧溢出。

## [0.4.2] - 2026-08-17

### 修复

- **检查更新不生效**：公开仓库地址之前依赖环境变量注入，实际从未配置，导致「检查更新」永远提示仓库未配置。已改为默认指向公开仓库 `moononnn/Hanako-tigexing`（环境变量仍可覆盖），点「检查更新」能真正查到最新版本。

## [0.4.1] - 2026-08-17

### 修复

- **方言/学我说话等扩展风格选中后保存丢失**：设置页选方言风格提示「已保存」，刷新后却变回系统默认。根因是落盘时的风格白名单用静态基础列表，方言等动态注册的扩展风格被静默丢弃；已改为动态白名单，方言能正常保存（同时修复保存失败仍提示「已保存 ✓」的误导）。

## [0.4.0] - 2026-08-17

### 🎉 首个正式分享版

提个醒从「自己用的弹窗小喇叭」打磨成可以分享给大家的正式版：三项通知全面接管、风格与音效自由搭配、分享版入口（检查更新 + 反馈）齐活。

### 新功能

- **全面接管 Hana 三项通知**：聊天回复完成、计划任务完成、巡检完成，各自独立档位（总是 / 不在 Hana 时 / 从不），跟 Hana 原版设置一一对应；同窗口 30 秒内多条回复自动合并防轰炸
- **带会话标题 + 助手头像**：一眼看出是哪个助手、哪个窗口回复了，多窗口同时开也不会懵
- **按助手个性化**：每个助手可独立配置通知风格 + 音效，自由组合；「看看效果」弹真实通知预览（音效 + 风格文案 + 头像三合一）
- **11 种通知风格**：跟随默认、简洁高效、活泼可爱、温柔贴心、闺蜜俏皮、古风书简、大厂黑话、摩斯电码、系统故障、锟斤拷；风格只包装标题和正文，正文永远显示助手原话
- **表情包插件联动方言风格**：装了【表情包】插件可解锁九种方言（东北/河南/上海/粤语/台湾腔/四川/陕西/北京/新疆）+「学我说话」；方言动态跟随，表情包新增方言重启即出现
- **自定义音效**：支持 wav / mp3 / m4a / aac；音效文件夹可自定义（「选择文件夹…」弹原生选择框，或手动填路径，留空用默认）；按助手分配，一耳朵听出是谁回复了
- **可选的文案润色**（默认关）：语气型风格（默认/简洁/活泼/温柔/闺蜜/古风/黑话 + 方言/学我说话）的标题正文由模型精修；失败或超时自动退回规则版，通知永不哑火；模型配置三档（跟随助手 / Hana 模型 / 自定义 API，Key 走系统 DPAPI 加密）
- **静默时段**：支持跨午夜（如 22:00 至 08:00），期间通知全部安静
- **只提醒该提醒的**：只认最终回复回合（工具调用中途不打扰）；只提醒用户真实参与的会话，插件后台调度不误弹
- **分享版入口**：设置页底部「关于提个醒」卡片，检查更新 + 反馈聊天窗（自动整理 issue 草稿）

### 修复

- 提个醒从不弹通知（根因级）：未导入函数导致每回合报错被吞，已补导入 + 异常落盘日志
- 自定义音效不生效：toast audio 不支持 file:// 自定义音频，改为 toast 静音 + PowerShell 独立播放（wav 走 SoundPlayer，mp3/m4a/aac 走 WinRT MediaPlayer）
- 设置页按钮全部失效 / 列表永远为空：请求未带凭证被宿主拒绝 + JSON 解析遗漏，页面整体重做
- 润色开关改完不生效：设置页与弹窗进程各自缓存配置不同步，改为文件签名感知、双实例即时同步
- 通知正文太长被系统截断：全局压缩到两行内可读，截断优先断在句子完整处；[mood] 等元信息块不再漏进正文
- 后台调度误弹：漂流瓶写瓶等后台会话不再被当成聊天通知打扰
- 「看看效果」预览弹错风格 / 新风格示例缺失：预览改读下拉当前选中值，闺蜜/古风补专属示例
- 下拉组件多处交互问题：选中后触发栏不刷新、emoji 窄栏显示不全、展开被下层卡片穿模盖住
- 中文音效文件名配置自动迁移，老配置无缝升级

### 优化

- 设置页翻新为纸感手帐风（薄荷绿 + 樱花粉 + 圆角 + 柔和阴影），删除合并间隔/通知历史/重要关键词等冗余设置
- 下拉栏全部换手帐风自定义组件，沉淀为通用积木（以后新插件直接复用）
- 方言联动动态跟随：不再写死方言列表，表情包加款删款都自动感知
- 通知正文长度按风格区分（默认 24 字、简洁 18 字），Windows 通知内两行放得下
- 零额外运行时依赖：纯 Node + Windows 自带 PowerShell，不需要 Python 或 C# helper